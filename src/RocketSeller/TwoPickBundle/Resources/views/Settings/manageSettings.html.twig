{% extends "::base.html.twig" %}

{% block title %}Preferencias{% endblock %}

{% block body %}
	<div id='registrationFormContainer' class=''>
		{{ form_start(form) }}

		
			{{ form_row(form.notificaciones) }}
			<div class="form-group">
				<label class="control-label required" for="settings_notificaciones">Redes Asociadas</label>
				<a href="/connect/facebook" class="btn btn-block btn-social btn-facebook {{redes['facebook']}}">
    				<span class="fa fa-facebook"></span>Sign in with Facebook
				</a>
				<a href="/connect/linkedin" class="btn btn-block btn-social btn-linkedin {{redes['google']}}">
    				<span class="fa fa-linkedin"></span>Sign in with Linkedin
				</a>
				<a href="/connect/google" class="btn btn-block btn-social btn-google {{redes['linkedin']}}">
    				<span class="fa fa-google"></span>Sign in with Google+
				</a>
			</div>

			<div class="form-group">
				<label class="control-label required" for="settings_notificaciones">Método de pago</label>
			</div>
		{{ form_end(form) }}
	</div>

{% endblock %}