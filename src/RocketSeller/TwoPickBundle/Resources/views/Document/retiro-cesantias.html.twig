<!DOCTYPE html>
<html lang="es">
<head>
<title>RETIRO CESANTIAS</title>
<meta charset="utf-8" />
<style>
body {
    height: 792px;
    width: 612px;
    /* to centre page on screen*/
    margin-left: auto;
    margin-right: auto;
}
</style>
<head>
<body>
<hr />
<p>&nbsp;</p>
{% set local = "now"|date("d-m-y") %}
{# <p><strong>{{ data.contract.city }}, {{ "now"|localizeddate('long', 'none', local) }}</strong><br />#}
<p><strong>{{ data.contract.city }}, {{ "now"|localizeddate('long', 'none', local) }}</strong></p>
<p>&nbsp;</p>
<p>Señores:</p>
<p><strong>{{ data.employee.afp.name }}</strong></p>
<p>Pensiones y cesantías</p>
<p>{{ data.contract.city }}</p>
<p>&nbsp;</p>
<p style="text-align: justify;">Respestados señores:</p>
<p style="text-align: justify;">De la manera más atenta nos permitimos informarles que el(la) Sr(a). {{ data.employee.name }}
identificado con {{ data.employee.docType }} número {{ data.employee.docNumber }}, trabajó en nuestra compañía hasta el {{ data.contract.fechaFin.date|localizeddate('long', 'none', local) }}</p>
<p style="text-align: justify">Por lo tanto les solicitamos se sirvan entregar la Cesantías consignadas del año {{ data.contract.fechaFin.date|date('Y') }}.</p>
<p>&nbsp;</p>
<p>Atentamente,</p>

_____________________________________
<p>{{ data.employer.name }}</p>
<p>{{ data.employer.docType }} Nº {{ data.employer.docNumber }}</p>

<p>&nbsp;</p>
<hr />

</body>
</html>