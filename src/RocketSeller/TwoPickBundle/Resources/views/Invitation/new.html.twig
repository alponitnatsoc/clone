{% extends '::base.html.twig' %}

{% block fos_user_content -%}
    <h1>Invitation creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a class="nav-link" href="{{ path('invitation') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
