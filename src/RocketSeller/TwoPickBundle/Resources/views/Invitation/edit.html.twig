{% extends '::base.html.twig' %}

{% block content -%}
    <h1>Invitation edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a class="nav-link" href="{{ path('invitation') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
