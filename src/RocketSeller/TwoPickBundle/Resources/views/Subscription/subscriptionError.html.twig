{% extends "::base.html.twig" %}
{% block title %}Symplifica - Activar Suscripción{% endblock %}

{% block stylesheet %}
    {{ parent() }}
    <style type="text/css">
        .media{
            padding: 10px;
            background-color: #DDD;
            display: -webkit-box;
        }
        .media-left{
            font-size: large;
            color: orange;
        }
        .media-heading{
            font-weight: bold;
        }
        .padding{
            padding: 50px 0px 50px 0px ;
        }
    </style>
{% endblock stylesheet %}
{% block fos_user_content %}
    <div class="col-sm-12 col-xs-12 align-Resp">
        <div id="" class="col-sm-12 col-xs-12 blockStep inline">
            <div id="" class="col-md-2 col-xs-12 txt-AD0" style="text-align: left;">
                <img src="/img/block-inline.png" class="center-block">
            </div>
            <div id="" class="col-md-10 col-xs-12 txt-AD1">
                <div id="" class="text-left">
                    <span class="step">Paso 3 de 3</span>
                    Subscripción a Symplifica
                </div>
            </div>
        </div>
    </div>
    <ul id="formNav" class="nav nav-tabs nav-justified">
        {% set tab = 2 %}
        <li {%   if tab is defined and tab==0 %} class="active" {% endif %}><a data-toggle="tab" data-target="#newOrOldTab"></a>Validar</li>
        <li {%   if tab is defined and tab==1 %} class="active" {% endif %}><a data-toggle="tab" data-target="#basicData"></a>Confirmar</li>
        <li {%   if tab is defined and tab==2 %} class="active" {% endif %}><a data-toggle="tab" data-target="#contactData"></a>Finalizar</li>
    </ul>
    <div class="row">
        <div class="col-sm-10 col-sm-offset-1">
            <div class="media">
                <div class="media-left">
                    <span class="glyphicon glyphicon-remove-circle fa-2x"></span>
                </div>
                <div class="media-body vertical-align-center" style="padding-top: 9px;">
                    <h4 class="media-heading">Error de pago</h4>
                </div>
            </div>
            <div class="text-center">
                <div class="padding">
                    <h4><b>El pago no puedo ser realizado</b></h4>
                </div>
                {% if user.dataCreditStatus ==4 %}
                    <div class="padding">
                        Desafortunadamente tu pago no ha podido ser procesado, ya que no se pudo validar tu identidad y exediste el numero de intentos permitidos en el día intenta de nuevo otro día.
                    </div>
                {% elseif user.dataCreditStatus ==3  %}
                    <div class="padding">
                        Desafortunadamente tu pago no ha podido ser procesado, ya que no se pudo validar tu identidad.
                    </div>
                {% else %}
                    <div class="padding">
                        Desafortunadamente tu pago no ha podido ser procesado, es posible que los datos de tu cuenta sean invalidos o que tuvieramos un problema con la comunicación con tu banco, por favor intenta de nuevo, y si el error persiste escríbenos un correo a ayuda@symplifica.com para evaluar tu caso.
                    </div>
                {% endif %}
                <div class="padding">
                    <input type="button" class="btn btn-orange" value="Intentar de nuevo"  onclick="window.location = '{{ path('subscription_choices') }}'" >
                </div>
            </div>
        </div>
    </div>

{% endblock %}