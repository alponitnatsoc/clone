{% extends "::base.html.twig" %}
{% block fos_user_content %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    <link rel="stylesheet" type="text/css" href="{{ asset('css/daviplata.css') }}"/>
    <div id="formFields" class="form-black">
        <div class="row">
            <div class="col-md-10 col-md-push-1">
                <br/>
                <br/>
                <div class="textoda">
                    <h2 class="h2">¿Cómo creo una cuenta en Daviplata para mi empleado?</h2>
                    <br/>
                    <p>Para crear la cuenta en Daviplata, te ofrecemos dos opciones, puedes dar clic en el botón del video y seguir los pasos o dar clic en el
                    botón de ir a daviplata, seguir las instrucciones y regresar a nuestra plataforma para activar la cuenta.</p>
                </div>
                  <div style="text-align: center;margin: 30px auto;width: 70%;">
                    <div>
                      {% if form.cellphone is defined %}
                          <div id="step2">
                              <p class="p">Ahora debes escribir el número que registraste en Daviplata para guardarlo en Symplifica</p>
                              {{ form_row(form.cellphone) }}
                              {{ form_row(form.save) }}
                          </div>
                      {% endif %}
                      {% if form.create is defined %}
                          <div id="step1">
                              {{ form_row(form.create) }}
                          </div>

                      {% endif %}
                      {% if form.discard is defined %}
                          <div id="step3">
                              {{ form_row(form.discard) }}
                          </div>
                      {% endif %}
                  </div>
                  <div>
                    <button style="width:100%" class="btn btn-default buttond">
                      <span id="davimoda">Ver paso a paso<span/>
                    </button>
                  </div>
                </div>
            </div>
        </div>
    </div>

    <!--modal daviplata-->

        <div id="triggermoda" class="modalito">

          <!-- Modal content -->
          <div class="modal-contenido">
            <span class="close1" style="color:#FFF;font-weight:bold;text-align:right;padding: 5px 20px 5px 0;font-size:30px;cursor:pointer">×</span>
            <iframe width="100%" height="95%" src="https://www.youtube.com/embed/2An52p799Fk" frameborder="0" allowfullscreen></iframe>
          </div>

        </div>
    <!--fin modal daviplata-->
    <script>
            // Get the modal
            var moda = document.getElementById('triggermoda');

            // Get the button that opens the modal
            var btn1 = document.getElementById('davimoda');

            // Get the <span> element that closes the modal
            var span1 = document.getElementsByClassName("close1")[0];

            // When the user clicks the button, open the modal
            btn1.onclick = function() {
                moda.style.display = "block";
            }

            // When the user clicks on <span> (x), close the modal
            span1.onclick = function() {
                moda.style.display = "none";
            }

            // When the user clicks anywhere outside of the modal, close it
            moda.onclick = function(event) {
                if (event.target == moda) {
                    moda.style.display = "none";
                }
            }
    </script>
    {{ form_end(form) }}
{% endblock fos_user_content %}
